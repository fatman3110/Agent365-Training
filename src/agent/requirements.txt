# エージェント本体の依存パッケージ（現行 A365 SDK に準拠）
openai                              # LLM（Ollama 互換）呼び出し。distro が自動計装する
python-dotenv                       # .env 読み込み

# Microsoft 365 Agents SDK（ホスティング）
microsoft-agents-hosting-core
microsoft-agents-hosting-aiohttp

# Agent 365 Observability（統一ディストロ。旧 microsoft-agents-a365-observability-* は非推奨）
microsoft-opentelemetry
